
# --- dual-agent setup (managed by setup-dual-agent skill) ---
.agents/local.json
.agents/generated/
.codex/
.claude/settings.local.json
# --- end dual-agent setup ---

# macOS
.DS_Store

# --- Swift / Xcode build artifacts ---
.build/
DerivedData/
# local unsigned .dmg output (make dmg)
dist/
Apps/*/build/
# XcodeGen output — project.yml is the source of truth (`make gen` regenerates)
Apps/*/*.xcodeproj
.swiftpm/
*.xcuserstate

# --- Python pipeline ---
pipeline/.venv/
__pycache__/
*.pyc
.ruff_cache/

# local-only secrets (Sparkle private key export); never committed
.secrets/
