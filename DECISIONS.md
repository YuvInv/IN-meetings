# Architectural Decisions

This file tracks significant architectural decisions made during development of IN-meetings. Both Claude Code and Codex CLI read this before making changes that affect project architecture. Append-only — do not edit or remove past entries.

## Format

```
### YYYY-MM-DD — <short title>
- **Agent**: Claude Code | Codex CLI
- **Decision**: What was decided
- **Rationale**: Why this approach was chosen
- **Alternatives considered**: What else was evaluated and why it was rejected
- **Consequences**: Tradeoffs accepted
```

---

<!-- Decisions will be added here as development progresses. Newest last. -->

### 2026-06-11 — MVP slice 2: global Start/Stop hotkey via Carbon (not Accessibility)
- **Agent**: Claude Code (confirmed by Yuval)
- **Decision**: The global Start/Stop hotkey (default ⌃⌥⌘R) uses Carbon `RegisterEventHotKey` — one registered chord, fires system-wide, routed to the app's event target. No dependency. `GlobalHotKey` in `INMeetingsCore/Input`.
- **Rationale**: `RegisterEventHotKey` registers a single specific chord and cannot observe any other input, so it needs **no Accessibility permission**. The alternative — `NSEvent.addGlobalMonitorForEvents` — receives every keystroke and is gated behind Accessibility: the scariest onboarding prompt, an enlarged privacy surface (keystroke-observation capability) for a confidential founder/deal recorder, and re-grant fragility tied to the code signature. We lose nothing — a single global chord is exactly what `RegisterEventHotKey` is for. Permission-light is an ADR-011 requirement.
- **Alternatives considered**: NSEvent global monitor + Accessibility (rejected — permission/privacy cost for a one-key feature); SwiftUI menu `.keyboardShortcut` (rejected — only fires when the app is frontmost, not global); SPM packages soffes/HotKey or sindresorhus/KeyboardShortcuts (deferred — both wrap the same Carbon API; a package buys a user-customizable recorder UI the MVP doesn't need, at the cost of a dependency — revisit if shortcut customization lands in settings).
- **Consequences**: fixed default chord for now; user customization is a later settings feature (would likely adopt KeyboardShortcuts then).
- **Also this slice (minor, no separate ADR)**: (a) the live recording timer lives in the **menu-bar status-item label** (model-driven 1s tick), not the dropdown — a `.menu`-style `MenuBarExtra` is a native `NSMenu` that won't tick a SwiftUI `Text(_, style:.timer)`; (b) the onboarding TCC wizard moved from slice 2 to **slice 3**, since the System Audio Recording + Microphone grants are only testable once capture exists; (c) local dev signs with **Apple Development** (sufficient to avoid the P2 TCC digital-silence gotcha) — **Developer ID + notarization deferred to Phase 5** rollout.

### 2026-06-11 — P2 prototype: dual-track capture VERIFIED (+ interleaved-write gotcha)
- **Agent**: Claude Code
- **Decision**: P2 (Core Audio process tap + AVAudioEngine mic → two WAVs) is verified live on M4/macOS 26.5 with **only the System Audio Recording permission** (no Screen Recording). Confirms ADR-002's capture architecture. MVP capture module to be built on this.
- **Rationale**: End-to-end proof — `✅ real audio (peak −2.7 dB)`; transcribing both tracks showed clean separation (mic = Hebrew test sentence, system = the English video, no cross-bleed). 1891 IOProc callbacks over 20s.
- **Implementation gotcha (must respect in MVP)**: the tap delivers **interleaved float32**; `AVAudioFile(forWriting:settings:)` defaults to non-interleaved → `write(from:)` fails with OSStatus **−50** and the file is silent despite the tap working. Fix: create the file with the tap's exact processing format (`commonFormat` + `interleaved:true`). Also: System Audio Recording TCC attaches to the **responsible process** — an unsigned binary launched headlessly gets silence; the real (Developer-ID-signed) app avoids this.
- **Consequences**: capture is de-risked. Remaining Phase-0 item: P4 (in-person diarization).

### 2026-06-11 — Recording modes: manual capture + in-person meetings (ADR-011)
- **Agent**: Claude Code (requirements from Yuval)
- **Decision**: Added **manual Start Recording** (menu-bar + global hotkey) and a first-class **in-person (mic-only) capture profile**. Manual start **auto-picks the profile** from the P3 audio-process detector (live call → dual-track; else → mic-only). In-person is **manual-trigger only** (no calendar auto-prompt) but gets **per-speaker diarization from v1**. New ADR-011; ADR-001/002/003/005 + DESIGN + IMPLEMENTATION_PLAN updated; `meeting_type` added to the package; new de-risk prototype **P4 (in-person diarization)**.
- **Rationale**: Manual covers the "auto-detect missed it" fallback and is the only viable trigger for face-to-face meetings (no app/remote audio to detect). In-person puts everyone on one mic, so the call-time 2-track me/them shortcut doesn't apply — single-track multi-speaker diarization is required and is a distinct, harder quality axis.
- **Alternatives considered** (Yuval chose): calendar auto-prompt for in-person (rejected — manual only); merged in-person transcript for v1 (rejected — per-speaker from v1); manual always asks call-vs-in-person (rejected — auto-pick from audio signal).
- **Consequences**: in-person diarization quality (Hebrew DER, overlap, room acoustics) is unproven → P4 prototype + dashboard manual speaker assignment as backstop. Global hotkey needs a permission-light mechanism. Downstream skills must handle packages with no `audio_system.wav` and all speakers on the mic track.

### 2026-06-11 — P3 prototype: call detection via Core Audio process I/O (not app/tab heuristics)
- **Agent**: Claude Code (course-corrected by Yuval)
- **Decision**: Meeting detection's primary signal is **Core Audio per-process audio I/O** — a process with **both input and output audio running** is a live call, identified by `kAudioProcessPropertyBundleID`. ADR-001 revised. The frontmost-app + AppleScript-tab-URL approach is dropped to optional enrichment.
- **Rationale**: Verified live on M4/macOS 26 — the app/tab approach **failed** (missed a backgrounded Google Meet call; required Automation TCC). The audio-process approach is how real call-recorders work: app-agnostic (Zoom/Meet/WhatsApp/Teams uniformly), **no Automation/Accessibility/Screen-Recording permission**, frontmost-independent, and it rejects one-way playback. Test: YouTube → `armed=no`; Meet w/ mic → `armed=YES CALL in: Google Chrome`.
- **Alternatives considered**: device-level mic-in-use (AirPods blind spot, no app id — rejected); frontmost-app + tab-URL (fragile, needs Automation, missed backgrounded calls — rejected as primary); browser extension (unnecessary given the audio signal).
- **Consequences**: API is macOS 14.2+ (fine — floor is 26). Edge case: a call muted-from-start is output-only and looks like playback → mitigate with a "seen-bidirectional" latch + calendar context (minor follow-up). Detection needs no TCC; capture (ADR-002/P2) still needs the audio-recording grant.

### 2026-06-11 — P1 prototype: context injection is post-correction, not prompt biasing
- **Agent**: Claude Code
- **Decision**: Based on the P1 ASR benchmark on real founder-meeting audio (M4/macOS 26, whisper.cpp Metal + ivrit-ai turbo GGML), the context-injection mechanism is **deterministic entity post-correction**, not Whisper `initial_prompt` biasing. ADR-003 and ADR-004 amended accordingly. Evidence: `pipeline/benchmarks/P1-FINDINGS.md`.
- **Rationale**: Measured — `initial_prompt` with Latin proper nouns *regressed* names the model otherwise got right (אלגוליון/Algolion → אלכוהוליון/"alcohol-ion", 6×); Hebrew-script terms were neutral; neither fixed the hardest term ("IN Venture"). The forced-Hebrew fine-tune won't emit Latin and its prompt-following is weak (matches the research's catastrophic-forgetting caveat). Post-correction (canonical entity + variant spellings → whole-token replace) was validated: deterministic, no regressions, and yields the canonical Latin spellings the Claude skills want.
- **Alternatives considered**: Latin-script `initial_prompt` (regresses — rejected); Hebrew-script `initial_prompt` (neutral — kept only as an optional light primer, no Latin terms); model fine-tuning for code-switching (out of scope for now).
- **Consequences**: ADR-004 emits a correction vocabulary (canonical + variants) rather than a ranked prompt word-list; the CI check becomes a post-correction fixture test; a fuzzy/edit-distance pass for unseen variants is the next refinement. On-device default is confirmed (speed + quality both pass).
- **Also confirmed GO**: on-device Hebrew ASR is fast (RTF ≈ 0.10, ~10× realtime) and high quality on the team hardware floor; beats Timeless on speaker attribution (Timeless gave only generic Speaker 1/2/3).

### 2026-06-11 — Design-phase ADRs (ADR-001 … ADR-010)
- **Agent**: Claude Code
- **Decision**: Recorded the full design in `DESIGN.md` + `adr/`. One-line summaries (each links to its ADR):
  - **[ADR-001](adr/ADR-001-call-detection-trigger-ux.md)** — Multi-signal detection (running-app ∪ mic-in-use ∪ calendar ∪ browser-tab via AppleScript); **prompt-to-record** (not silent auto) with a **ring-buffer** so pre-confirm audio isn't lost; non-activating `NSPanel` over fullscreen; menu-bar `LSUIElement`.
  - **[ADR-002](adr/ADR-002-capture-pipeline.md)** — **Core Audio process tap** (remote/system, per-process) + **AVAudioEngine** (mic) as **two WAV tracks**; raw mic + **offline AEC**; **video off by default** (SCK/HEVC opt-in); mic-idle-debounced auto-stop. No virtual driver, no Screen Recording TCC for audio.
  - **[ADR-003](adr/ADR-003-transcription-engine.md)** — Primary **`ivrit-ai/whisper-large-v3-turbo`** on whisper.cpp Metal (GGML), MLX optional; `initial_prompt` biasing (≤224 tokens, terms last, pinned revision + regression test); **senko** diarization on the system track only; `transcript.json`+`.txt`; **Soniox** opt-in cloud fallback; Vision OCR for slides (opt-in).
  - **[ADR-004](adr/ADR-004-context-assembler.md)** — Match meeting → Calendar event → Saventa + Dealigence → **ranked biasing vocabulary** + `context.md` (priors only); graceful degradation when unmatched.
  - **[ADR-005](adr/ADR-005-context-package-contract.md)** — Freeze the package schema as the contract; add a **`--package <path>` local-input mode** to `saventa-summary`/`generate-mom`/`process-meeting` alongside the Timeless path; richer fields enable Hebrew-name, slide, and confidence improvements.
  - **[ADR-006](adr/ADR-006-storage-drive-sync.md)** — Local **SQLite + per-meeting folder** (cache); **Google Shared Drive via per-user OAuth** as durable source of truth; **company-first** `/<Company>/<YYYY-MM-DD-…>/` layout; write-through upload (no sync client).
  - **[ADR-007](adr/ADR-007-dashboard.md)** — **SwiftUI window in the same app** over SQLite (FTS5 search); list/detail/play/open-in-Drive/re-run-skill. No separate web app.
  - **[ADR-008](adr/ADR-008-claude-auto-trigger.md)** — Headless **`claude -p` chain**, configurable per meeting, **default one-click**; meeting-type routing decides which skills run; `process-meeting`'s human-review gate kept.
  - **[ADR-009](adr/ADR-009-app-architecture-ipc.md)** — **Swift menu-bar app + bundled Python pipeline**, **file-based job queue + JSON status** IPC (simple/observable/resumable); **internal Developer-ID signing** (not App Store) to allow the audio-permission SPI; onboarding wizard for the 4 TCC grants + Drive OAuth.
  - **[ADR-010](adr/ADR-010-privacy-consent.md)** — On-device default; only the team Drive (and opt-in cloud/Claude) leave the Mac; consent metadata + jurisdiction nudge (inform, don't block) + retention controls + **do-not-record defaults for internal events**. Six items flagged for **counsel**.
- **Rationale**: Per the design brief and `RESEARCH.md`; load-bearing claims verified against primary sources (Apple Core Audio docs, ivrit.ai leaderboard, model licenses).
- **Alternatives considered**: Documented per-ADR (e.g., SCK-for-audio rejected for the monthly-nag/Screen-Recording-TCC cost; cloud-ASR-primary rejected — no Hebrew quality win + confidentiality cost; service-account Drive auth rejected in favor of per-user OAuth).
- **Consequences**: Code-switching ASR quality and the no-monthly-nag tap claim are unproven until prototyped; skills in `~/repos/claude-skills` need a coordinated `--package` change; internal signing means maintaining Developer ID + notarization.

### 2026-06-11 — MVP slice 4c: diarizer = senko (bake-off-validated on Hebrew), profile-aware speaker attribution
- **Agent**: Claude Code (confirmed by Yuval)
- **Decision**: **senko** (CoreML/ANE, MIT) is the diarizer — confirming ADR-003's choice, but now settled by **data, not assumption**. Ran a senko-vs-pyannote **bake-off** on real Hebrew founder-meeting audio (`prelligence`): senko matched **Timeless ground-truth speaker counts at both scales** (2 on the 6-min clip, **3 on the full 51-min**) at RTF ~0.001–0.004. Speaker attribution is **profile-aware** (`attribute_speakers` in `__main__`): **call** → mic = `"Me"` (internal) + diarize the **system** track into external `"Them"` (1 speaker) / `"Speaker N"` (≥2); **in-person** → diarize the **mic** track into `"Speaker N"`. Each segment is attributed to the diarized speaker by **max temporal overlap** (`diarize.assign_speakers`); a `speakers[]` table (`{id, side, track}`) + `diarized` flag are added to `transcript.json`. Diarization **degrades gracefully** (coarse labels + `diarized:false`) rather than failing the transcript.
- **Rationale**: senko's only headline edge — raw ANE speed — is **moot for our batch pipeline** (whisper dominates wall-clock), while its feared weakness (embeddings trained on English+Mandarin → unvalidated Hebrew) **did not materialize**: it matched ground truth twice. pyannote installed cleanly (torch 2.12/MPS) but its `community-1` pipeline is **HuggingFace-gated** — making it primary would bake a per-user HF token + model-terms acceptance into onboarding for all ~5 users of an on-device tool. Rejected as primary on that deployment-friction; kept as the documented fallback (ADR-003).
- **Alternatives considered** (Yuval chose the bake-off, then "just use senko"): pyannote `community-1` (gated; fallback only); FluidAudio Swift-native CoreML (deferred — larger architecture change, would remove the Python diarization hop); cloud diarization Soniox/Deepgram (opt-in only — ADR-010).
- **Consequences**: senko requires **Python <3.14** (system `python3` is 3.14) → the pipeline is pinned to a **3.11 venv** (`pipeline/.venv`, deps in `pyproject.toml` + `uv.lock`), and **`JobBridge.defaultPython` now spawns `.venv/bin/python`** (still overridable via `IN_MEETINGS_PYTHON`; Phase 5 bundles a sealed env). DER (turn-boundary accuracy) is sane-by-eye but **not numerically measured** — count + attribution are validated against Timeless; a DER harness vs a hand-labeled reference is a follow-up. The **call path** was live-verified on a *synthetic* 2-track meeting (no real separated dual-track recording exists yet — re-verify on a real multi-party call when one is recorded).
- **MVP sign-off (Yuval, 2026-06-11)**: diarization is **accepted for the MVP without a live multi-party-call test** — Yuval can't record one right now and will flag it if it's not good enough once real calls run. To make that judgeable, every job's stdout+stderr is captured to a **per-meeting `pipeline.log`** (`JobBridge` redirects the child; `__main__` emits a `diarization profile=… diarized=… speakers=… talk_s=…` summary line), so real calls leave a reviewable trail. **⏳ PENDING: confirm multi-party-call diarization quality on a real recording** (tracked in `HANDOFF.md`).

### 2026-06-14 — Evaluated `island-io/mila`: continue ours, harvest commodity wins (Apache-2.0)
- **Agent**: Claude Code (decision by Yuval)
- **Decision**: Evaluated `island-io/mila` — a mature, just-open-sourced **Apache-2.0** macOS Hebrew transcription app (Island Technology / Uri Harduf). **Do NOT fork.** Continue our architecture and **harvest four commodity features** with attribution: (1) model-download-on-launch, (2) packaging + Sparkle auto-update, (3) "Record now" prompt overlay, (4) polished SwiftUI dashboard views. Full plan: `~/.claude/plans/i-have-a-questions-cached-lark.md`.
- **Rationale**: mila's mature parts are either undifferentiated commodity (UI, model download, packaging — worth lifting) or **incompatible with our hard constraints**: it captures via **ScreenCaptureKit** (needs Screen-Recording TCC + a placeholder video stream), **mixes mic+system into one mono track** (which kills our "Me vs Them" attribution), and diarizes with **pyannote + bundled torch** (its changelog is ~10 releases of torch/notarization firefighting — independent validation of our **senko** choice). Our moat (context injection, dual-track attribution, the Claude-skill package, Drive sync) is **0% built in mila**, so forking buys no shortcut while forcing us to re-fight two architectures we deliberately rejected. Apache-2.0 lets us lift specific files with attribution.
- **Alternatives considered** (Yuval chose "continue ours + harvest"): fork mila and bolt our features on (rejected — re-fight ScreenCaptureKit→Core Audio, mono→dual-track, pyannote→senko on a foreign ~80-file codebase); ignore mila entirely (rejected — reinvent the model-download/packaging/auto-update mila already nailed).
- **Consequences**: new Apache-2.0 attribution surface (`NOTICE`, `THIRD_PARTY_NOTICES.md`, `licenses/mila-LICENSE-2.0.txt`, per-file `// Adapted from Mila …` headers). Harvests are **additive** and must not block the moat work (slice 5 schema, slice 6 Drive, Phase 2 context assembler). **Harvest 1 (model download on launch) implemented this session.** mila's CoreML/ANE encoder is **inert for us** (the Homebrew `whisper-cli` is built without CoreML) — deferred to a Phase-5 bundled `WHISPER_COREML` build. We keep the `ivrit-large-v3-turbo` model (not mila's full `large-v3`) to preserve the P1 WER/RTF baseline. Harvest 2 (Sparkle) is **gated on Developer ID + notarization + EdDSA keys** (Phase-5 prerequisites).

### 2026-06-14 — UX standard: Liquid Glass + current SwiftUI for all app UI
- **Agent**: Claude Code (directive from Yuval)
- **Decision**: Every user-facing UX surface in the macOS app uses Apple's **Liquid Glass** design language (macOS 26 "Tahoe") and current SwiftUI idioms — for a best-in-class native feel. Scope: the **"Record now" overlay banner** (Harvest 3), the **dashboard / detail / settings** (Harvest 4), and any new UI. The menu-bar dropdown stays a native `NSMenu` (`.menu` `MenuBarExtra`, system-styled — not a glass surface).
- **Rationale**: the app competes with mila's polished UX; native-and-current is the bar Yuval set. The **app target deploys to macOS 26.0**, so Liquid Glass APIs are available — the `INMeetingsCore` library stays `.macOS(.v14)`, but all SwiftUI views live in the macOS-26 app target, so keep glass code out of Core.
- **How to apply**: prefer `.glassEffect()` / `GlassEffectContainer` and glass button styles (`.buttonStyle(.glass)` / `.glassProminent`) over legacy `.ultraThinMaterial`; the "Record now" overlay is a non-activating `NSPanel` hosting a SwiftUI glass view; respect RTL-Hebrew layout and glass contrast/accessibility (honor Reduce Transparency).
- **Consequences**: H3/H4 are built to this standard from the start; anything that must run pre-26 would need a material fallback (not a concern — the team floor is macOS 26).

### 2026-06-14 — Call video capture (ScreenCaptureKit, on by default) + "meeting done?" auto-stop prompt
- **Agent**: Claude Code (decisions by Yuval, during H3 live testing)
- **Decision**:
  1. **Record call video, ON by default for the `call` profile.** Add a ScreenCaptureKit **video-only** stream (`capturesAudio = false` — audio stays on our Core Audio dual-track path) scoped to the **detected call app's window** (SCK per-app `SCContentFilter`), written as **HEVC** alongside `mic.wav` / `system.wav`. Reuses the per-app SCK pattern (cf. mila's `SystemAudioRecorder`, but a real video stream, not a 2×2 placeholder). The **in-person** profile stays audio-only.
  2. **Auto-stop is a PROMPT, not a silent stop.** On the detector's **armed→idle** edge while a call recording is in progress, float the H3 Liquid Glass card as "Meeting ended — stop & process?". **Debounced** (call must stay idle ~10–15 s to ride out network blips) and **safe-by-default**: if ignored, keep recording — never silently drop a meeting.
  3. **Sequencing**: both land **after the core spine** (slice 5 package schema → slice 6 Drive → Phase 2 context injection). Additive; must not block the moat.
- **Rationale**: video gives the full meeting record (participants + shared screen/slides) the VC workflow wants; **window-only** capture keeps unrelated/sensitive screen content out. Prompt-to-stop avoids silently ending a recording mid-meeting on a transient audio drop.
- **Consequences (important)**: video-on-by-default **reverses the call path's "permission-light / no Screen Recording TCC" posture** (ADR-002 / P3) — **every user must now grant Screen Recording** (the ADR-009 onboarding/TCC wizard gains this grant; detection + audio still need no Screen Recording). **Privacy** (ADR-010): all calls are filmed by default → revisit consent/retention defaults + the do-not-record list; **flag for counsel**. **Storage** grows to GB-scale/meeting → needs a retention/size cap (cf. mila's storage-cap setting). **Amends ADR-002** (capture adds an SCK video track), **ADR-009** (onboarding +Screen Recording), **ADR-010** (video consent/retention) — update those ADRs when the slice is built.
- **Alternatives considered** (Yuval chose): opt-in video (rejected — wants it always for calls); full-display capture (rejected on privacy — chose call-window-only); silent auto-stop (rejected for MVP — chose prompt-for-approval).

### 2026-06-14 — MVP slice 5: context-package contract frozen (`schema/`) + SQLite index (GRDB)
- **Agent**: Claude Code (directive from Yuval: "work by the plan order")
- **Decision**: Froze the ADR-005 context-package contract as machine-readable **JSON Schema** in `schema/` (`transcript.schema.json` + `metadata.schema.json` + `README.md`) and added the ADR-006 local **SQLite index via GRDB**. Sub-decisions:
  - **`transcript.json` shape = ADR-005 (option B1)**: pipeline `segments[]` → `utterances[]`; `start_ms`/`end_ms` int-ms → `start`/`end` float-**seconds**; inline `speaker` label → `speaker_id` referencing `speakers[].id`; added `confidence` (null at MVP) + provenance `engine`/`model_revision`/`biased`. Kept additive extras (`speakers[].track`, top-level `profile`/`diarized`/`meeting_id`) — consumers ignore unknowns.
  - **The Python pipeline is the single writer of the package** (ADR-009). Swift hands record-time facts via an extended `job.json` (`started_at`/`ended_at`/`capture_source_app`); sample-rate + durations are read from the WAVs (soundfile), and `video` defaults off (V1). `metadata.py` merges these with transcription facts; Phase 2's context assembler (also Python) extends `metadata.json` in place.
  - **Forward-compatible by design**: `metadata.json.schema_version` = "1.0"; every Phase-2 / slice-6 / V1 field is present-but-null/empty at MVP (calendar/CRM/consent, `video`, `confidence`) so freezing now doesn't force a breaking bump when those land.
  - **Meeting id = the local timestamp folder** (`yyyy-MM-dd_HH-mm-ss`, company-independent); company is metadata and only drives the Drive folder path (slice 6). The immutable id never re-keys on a late company match.
  - **SQLite index = GRDB** (ADR-006) — the **first external dependency** in the previously dependency-free `INMeetingsCore` SPM package, pinned to the 6.x line (Swift 5.9 toolchain). `MeetingStore.indexPackage(at:)` upserts a `meeting` row (idempotent) when the pipeline reports `done`; FTS5 + the dashboard are deferred to H4.
  - **Golden fixture** `schema/fixtures/golden-package/` is the single cross-language / cross-repo contract anchor: Python `jsonschema`-validates the schemas, the fixture, AND the pipeline's emitted output; Swift decodes the fixture into its Codable models; the skills repo mirrors it for the `--package` adapter.
- **Rationale**: the package is the moat — the stable contract the Claude skills consume — so freezing it correctly while there are *zero* real consumers is cheap and unblocks slice 6 (Drive), Phase 2 (context injection), and the dashboard. B1 normalizes to the shape the skills + Timeless already expect. Single-writer (Python) keeps assembly race-free and localizes Phase 2's later edits.
- **Alternatives considered**: freeze the existing `segments`/int-ms/inline-speaker shape and translate in each skill's adapter (rejected — bakes a less-normalized shape into a permanent contract); Swift seeds `metadata.json` + Python merges (rejected — two-writer merge races; the pipeline already owns the folder); meeting id = date+company+sequence (rejected — company is unknown pre-match and would re-key the folder/id on a late match); raw `SQLite3` instead of GRDB (rejected — ADR-006 chose GRDB; deviating would be an undocumented architecture change).
- **Consequences**: `INMeetingsCore` now depends on GRDB (transitive in the app; first build fetches it). Tests: Python **17** (transcript/metadata/contract), Swift **21** (ContextPackage decode, MeetingStore, JobBridge contract); `make build-mac` green; ruff clean. **⏳ PENDING LIVE VERIFICATION**: the end-to-end record → package → **index** path is untested on a real meeting (none recorded this session) — the unit/contract layer is green; a live run is the next check (HANDOFF). The skills `--package` adapter in `~/repos/claude-skills` remains a separate coordinated change (ADR-005 part 2, Phase 3).
- **Slice 5 LIVE-VERIFIED (2026-06-14)**: recorded a real Google Meet call → the DB row showed `type=call`, real ISO timestamps + 26.6s duration, `speakerCount=2`/`diarized=1`, `model_revision=ivrit-large-v3-turbo`, `capture_source_app="Google Chrome (Meet/web call)"`, `syncState=local`. The full chain (extended job.json → metadata.json → SQLite index) works end-to-end. Merged via PR #4. (Multi-party 3+ diarization quality is still the separate open follow-up from slice 4c.)

### 2026-06-14 — MVP slice 6: Drive sync — Swift-owned (amends ADR-009) + per-user dynamic backup location (refines ADR-006)
- **Agent**: Claude Code (decisions by Yuval)
- **Decision**: Per-user Google Drive write-through sync, **Swift-owned** — this **amends ADR-009** (which slotted Drive sync under the Python pipeline). The whole engine lives in `Sources/INMeetingsCore/Drive/`: PKCE OAuth (`PKCE`, `GoogleOAuth`), Keychain token storage (`DriveCredential` + `KeychainTokenStore`, with refresh-token carry-forward), live token POST (`GoogleTokenService`), refresh (`DriveTokenManager`), a Shared-Drive-aware REST client (`DriveClient`), company-first upload orchestration (`DriveSync` → `<Company>/<meeting>/`, idempotent), per-user location persistence (`DriveLocationStore`), and `DriveBackup` wired into `JobBridge` to auto-upload on pipeline `done`. The only app-target piece is the interactive sign-in + location picker (`DriveAuth` via `ASWebAuthenticationSession`, surfaced in the menu).
  - **Backup location is per-user + dynamic** (Yuval's correction, refines ADR-006): each teammate connects their own IN Venture Google account and **picks their backup Drive** in-app (persisted via `DriveLocationStore`); nothing about the destination is hardcoded. The app creates an `IN Meetings/<Company>/<meeting>/` tree under whatever Shared Drive the user selects. The only static config is the app's **OAuth client ID** (`DriveConfig` — non-secret; PKCE has no client secret).
  - **Scope** (revised 2026-06-14, per Yuval — the recordings are the point of the backup): the text package (transcript.json/.txt, metadata.json, context.md) uploads in one request each, and the **recordings (mic.wav/system.wav, + video.mov when present) stream via a resumable Drive session** (`DriveClient.uploadFileResumable`) so large files never load into memory. Drive ids + sync state fill the index's existing `driveFolderId`/`syncState` columns. A retention/size cap (ADR-010) is the remaining follow-up.
  - **OAuth client type = iOS** (PKCE + reversed-client-id custom scheme, no secret) — the right fit for `ASWebAuthenticationSession`; scope = full `drive` (to navigate the team's shared folders across machines; `drive.file` is the tighter option if ownership changes).
- **Rationale**: OAuth + token (Keychain) + the upload trigger are all Swift-native; keeping the secret in-process (never handed to a spawned subprocess) and updating the index directly beats crossing the process boundary. Putting everything except the sign-in sheet in Core keeps it unit-testable (**19 Drive tests**) and lets `JobBridge` auto-sync with no app-layer involvement.
- **Alternatives considered**: Drive under the Python pipeline (original ADR-009 — rejected: hands the OAuth token to a subprocess + crosses the SQLite-ownership boundary); a single hardcoded shared Drive (original ADR-006 framing — rejected per Yuval: per-user dynamic choice); GoogleSignIn SDK (rejected — heavy; hand-rolled PKCE is ~1 file, keeps GRDB the only dependency); a single mixed-down upload (rejected — keep mic/system separate to preserve the Me/Them split).
- **Consequences**: 19 new Core unit tests (PKCE/OAuth, token store + refresh, client request-building, sync orchestration, location store, backup no-op) — **42 Swift tests total**; `make build-mac` green. Adding a new *app-target* file requires `make gen` (XcodeGen only auto-regenerates when `project.yml` changes). **⏳ PENDING LIVE VERIFICATION**: the interactive Google sign-in is untested (needs a real account); in particular the `ASWebAuthenticationSession` presentation anchor in a menu-bar (`LSUIElement`) app is a known-tricky spot that may need a live tweak (fallback: a browser redirect via the registered URL scheme). Amends ADR-009 + ADR-006 — update those ADR files when convenient.

### 2026-06-14 — PLANNED (pre-rollout): merge tracks into a single playback file (audio, or audio+video)
- **Agent**: Claude Code (requirement from Yuval) — **NOT yet implemented; required before team rollout.**
- **Requirement**: We record dual tracks (`mic.wav` = "Me", `system.wav` = "Them") — and, for video calls, a window video — purely to make transcription/attribution clean (ADR-002/011). But anyone who wants to **listen to or watch** a meeting should get the full, natural experience: a **single audio file** (mic+system mixed) for audio meetings, or a **single video file with the audio muxed in** for video calls. They should not see or care about the separate channels.
- **Recommended approach (the "where" + "how" Yuval asked for)**:
  - **Where**: a dedicated **"playback render" step in the Swift app using AVFoundation**, kicked **right after capture (on Stop)**. It needs only the raw tracks — *not* the transcript — so it can run **concurrently with the Python transcription pipeline** (the slow part). It produces a playback artifact *alongside* the transcription tracks; both finish around the same time.
  - **Why AVFoundation (Swift), not ffmpeg (Python)**: AVFoundation muxes video+audio **and** mixes/level-balances audio natively, with **no new PATH dependency** — the pipeline deliberately avoided ffmpeg (diarize.py normalizes via soundfile/scipy). The app already owns media *capture* (AVAudioEngine, SCK), so media *production* belongs there too. Tradeoff: unlike the Python pipeline, changing merge behavior needs an app rebuild — acceptable since the mix/mux logic is stable.
  - **Output**: `audio.wav` (mic+system, **level-balanced** so neither side dominates — the two tracks are captured at different gains; match RMS/peak via `AVAudioMix`) for audio meetings; `meeting.mp4` (call-window video + the mixed audio, A/V-aligned by capture start time, via `AVMutableComposition`/`AVAssetExportSession`) for video calls.
  - **Keep the separate tracks** as the transcription inputs (the moat — dual-track Me/Them attribution depends on them); the merged file is purely the human playback artifact.
  - **Drive backup (supersedes the current interim behavior)**: once this lands, **slice 6 should upload the merged playback file** (`audio.wav` / `meeting.mp4`) as the listen/watch artifact, **instead of** the separate `mic.wav`/`system.wav` (which are transcription inputs, not the experience). The dashboard player (H4) plays the merged file.
- **Sequencing summary**: Stop → (a) spawn the transcription pipeline [existing] and (b) kick the AVFoundation render [new], concurrently → both land in the meeting folder → the merged file is what's uploaded (slice 6) and played (H4).
- **Open**: stereo placement (Me left / Them right) vs a balanced mono mix — default to balanced; revisit if users want channel separation. Retention (ADR-010): once the merged file exists + is uploaded, the raw tracks can be pruned locally.

### 2026-06-14 — Phase 2 slice 1: calendar context assembler — Swift fetch / Python transform (amends ADR-004)
- **Agent**: Claude Code (design by Yuval; Yuval delegates technical/architecture decisions, sets behavior)
- **Decision**: First Phase-2 sub-slice = **Google Calendar only**. Swift fetches candidate events (slice-6 Google OAuth + new `calendar.events.readonly` scope) and writes `<meeting>/context.input.json` *before* spawning the pipeline; the Python pipeline matches the event (max time-overlap with the recording window), splits internal/external by the **signed-in account's domain**, builds the post-correction vocab (`context.vocab.json`, consumed by the existing `load_vocab`→`postcorrect` hook), renders `context.md` (priors, with the priors/content wall), and merges fields into `metadata.json` (`meeting.title`/`calendar_event_id`, `attendees[]` with side, `company{matched:false}`, `context.sources.calendar`). The curated **core lexicon** (the fund name "IN Venture" + its P1-observed manglings) is applied on *every* meeting; per-company variants are best-effort transliterations (partial by design — variant accumulation deferred). Personal names are priors only (not rewritten, to avoid corrupting the transcript). Post-correction was hardened to **whole-token** (boundary-aware) replacement.
- **Amends ADR-004**: (a) the assembler is **not** "the Python pipeline calling MCP servers" — a headless subprocess can't reach MCP; credentialed fetch is **Swift-side** (reusing slice-6's Google OAuth). (b) "Run parallel with capture so the vocab is ready before ASR" is **relaxed** — P1 made deterministic post-correction (post-ASR) the mechanism, so the assembler is a post-spawn pipeline step. Saventa + Dealigence deferred to **slice 2**.
- **Rationale**: secrets stay in Swift (consistent with slices 5–6 — never hand a token to a spawned subprocess); Python stays the single package writer and owns the match/transform logic + fixture tests next to `postcorrect.py`/`metadata.py`. New module `pipeline/in_meetings_pipeline/context_assembler.py` + `data/core_lexicon.json`; new Core `Sources/INMeetingsCore/Calendar/` (`CalendarClient`, `CalendarContext`).
- **Alternatives considered** (Claude chose): everything-in-Python with a handed-in token (rejected — hands a secret to a subprocess, the thing slice 6 rejected for Drive; duplicates token refresh); Swift does the matching too (rejected — splits assembler logic across languages + more app-target code; matching is fixture-testable in Python); global static vocab with no calendar match (rejected — loses per-meeting attendee/company context + risks noisier replacements).
- **Consequences**: adding the calendar scope forces a **one-time Google re-consent** (also the moment to live-verify the still-unverified slice-6 sign-in). **No schema change** (slice 5 reserved every field). No new dependencies. Tests: **Python 45, Swift 48, `make build-mac` green**. Spec + plan: `docs/superpowers/specs/2026-06-14-phase2-calendar-context-design.md`, `docs/superpowers/plans/2026-06-14-phase2-calendar-context.md`.
- **Slice 1 LIVE-VERIFIED (2026-06-14)**: required enabling the **Google Calendar API** in GCP project 1062382667236 (it was off — the scope was granted + Drive worked, but Calendar returned 403 "API disabled"). After enabling, a real call matched its calendar event end-to-end (`context.md` attendees/company by side, `metadata.context.sources.calendar:"ok"`, names corrected, no fabricated remote speech). **Two live-test bugs fixed en route** (committed): (1) **silence-gating** (`asr.is_silent`) — a solo Meet's all-zero remote track made ivrit-whisper hallucinate Knesset boilerplate attributed to "Them"; silent tracks are now skipped for ASR + diarization. (2) **calendar error surfacing** — a fetch 403 now lands as `calendar:"error"` + a `context.md` note, not a silent "no match". Merged via PR (`feat/phase2-calendar-context`).

### 2026-06-14 — Scope: Saventa + Dealigence enrichment deferred out of the MVP (→ Phase 6)
- **Agent**: Claude Code (decision by Yuval)
- **Decision**: The context assembler's **Saventa (CRM) + Dealigence (founder) enrichment is removed from the MVP and pushed to the end of development (new Phase 6).** The MVP's context layer is **Google Calendar only** (Phase 2 slice 1, shipped). Calendar gives attendees, the internal/external split, and a domain-derived company name — enough for the differentiator (better Hebrew names + `context.md` priors).
- **Rationale**: calendar context is live-verified and self-sufficient for the MVP; Saventa/Dealigence add authoritative names + founder priors but aren't needed to ship value, and each is another credentialed integration. Deferring keeps the MVP lean and lets app UX + ASR-robustness (VAD) work come first.
- **Consequences**: `metadata.company` stays `matched:false` (domain-derived) until Phase 6; `sevanta_deal_id`/`dealigence_id` stay null; per-company correction relies on best-effort transliteration + accumulated variants until then. ADR-004's three-source design is unchanged in intent — only the *sequencing* moves. Updated IMPLEMENTATION_PLAN Phase 2 + new Phase 6.

### 2026-06-14 — Adopt whisper.cpp Silero VAD to stop within-track silence hallucination (amends ADR-003)
- **Agent**: Claude Code (decision by Yuval — "go on with the VAD")
- **Decision**: The ASR runs whisper.cpp with `--vad` (Silero `ggml-silero-v5.1.2`, ~865 KB) when the VAD model is present, so only detected speech is transcribed. Complements `asr.is_silent` (the whole-track gate): is_silent skips fully-silent tracks cheaply (no model needed); VAD handles silence *within* an active track. The model is app-provisioned by a second `ModelManager(entry: .sileroVad)` (download + SHA-256 verify on launch) and handed to the pipeline via `IN_MEETINGS_VAD_MODEL`; `asr.resolve_vad_model` gates on its presence so a box without it degrades to is_silent-only (never a hard failure).
- **Validated (`pipeline/benchmarks`)**: on the real all-zero `system.wav`, VAD → **0 segments** (vs the Knesset-boilerplate hallucination without it); on the P1 `prelligence_6min.wav`, VAD **preserved all proper nouns** (Prelligence, Algolion, General Motors, Technion) + coherent speech — no content lost, so the P1 WER baseline holds.
- **Alternatives considered**: `--suppress-nst` / `--no-speech-thold` tuning (cheaper, no model, but weaker than true VAD); a Python RMS-windowed gate (reinvents VAD). Chose whisper's built-in Silero VAD — proven + maintained.
- **Consequences**: a second tiny model download on first launch; `--vad` adds minor per-file overhead. Amends ADR-003 (transcription engine gains VAD). The fully-silent fast-path (`is_silent`) stays as the no-model floor.

### 2026-06-14 — App UX slice 1: merged playback file + mila-faithful Liquid Glass dashboard + settings
- **Agent**: Claude Code (UX direction by Yuval; structure modelled on `island-io/mila` after reading its source)
- **Decision**: Built the first dashboard UX (ADR-007 / mila Harvest 4) as a **`NavigationSplitView`** (sidebar: All Meetings / Needs linking / Processing + Settings) → **date-bucketed** meeting list → **detail** (RTL Hebrew transcript, tap-a-line-to-seek, AVKit player) → **in-memory search** → a **tabbed Settings** scene (Recording / Model / Drive), all in **Liquid Glass**, **English chrome / Hebrew RTL content**. Foundation: a **merged playback file** — `PlaybackRenderer` mixes `mic.wav`+`system.wav` → one level-balanced **`audio.m4a`** (AAC) at Stop, concurrent with the pipeline; **Drive now uploads `audio.m4a`** (the listen artifact), falling back to the raw tracks only if the render failed. Supersedes ADR-007's "standard SwiftUI / materials" aesthetic with Liquid Glass.
- **Rationale**: read mila's actual views — its sidebar+content+date-bucketed+rich-detail structure beats a bare two-pane, and mila plays **one** audio file per recording (validates the merge). Reused mila's date-bucketing, in-memory search, and transcript seek-row (Apache-2.0, attributed in `THIRD_PARTY_NOTICES.md`); rebuilt the rest against our `MeetingRecord`/`TranscriptPackage`/SQLite + `@Observable`.
- **Scope (deferred to later slices)**: company **folders** + drag-drop, **trash**, the **AI overview** (Phase-3 Claude-skills output), re-transcribe / export-SRT / send-to-LLM, **FTS5** (in-memory filter suffices at our scale), extra settings tabs (Audio device / Storage / Updates / Hotkey rebind), and **video** (`meeting.mp4` — V1 capture isn't built; the renderer is structured to add it).
- **Consequences**: `audio.m4a` is the new playback + Drive artifact; raw WAVs stay as the lossless transcription inputs (retention/size cap is a follow-up). New `Sources/INMeetingsCore/{Capture/PlaybackRenderer,Dashboard/MeetingBucketing}.swift` + `Apps/INMeetings/INMeetings/{Dashboard,Settings}/*`. **57 Swift tests + `make build-mac` green** (built via 3 parallel subagents + coordinator convergence). ⏳ PENDING LIVE VERIFICATION. Spec/plan in `docs/superpowers/`.
- **App UX slice 1 LIVE-VERIFIED + merged (2026-06-15)**: dashboard browses meetings, plays the merged `audio.m4a`, renders RTL Hebrew; tabbed settings work; Drive uploads `audio.m4a`. Merged to `main` via **PR #7 (`46e5fe9`)**, which also carried the VAD + Phase-6-scope-defer commits `main` was missing.

### 2026-06-15 — Post-MVP scope + priority order for a team-ready v1 (video pulled up; no in-app AI overview)
- **Agent**: Claude Code (priorities + scope by Yuval; full gap analysis this session)
- **Decision**: With the MVP spine complete and on `main`, set the ordered roadmap to a basic, reliable, team-installable recorder — full list in `IMPLEMENTATION_PLAN.md` → **"Road to a team-ready v1"**. Tiers: **P0** installable + trustworthy (hybrid Dock+menu-bar app shell, Developer-ID sign/notarize/`.dmg`, launch-at-login, onboarding/TCC wizard, reliability pass) → **P1** value loop + **video** (company-name inference + edit/rename UI, Claude auto-trigger → Saventa CRM, video capture → `meeting.mp4` → Drive → playback, retention cap, Drive folder picker) → **P2** polish. **Video is pulled up into P1** (Yuval flagged it "BIG"); the retention/size cap rides with it. **Out of scope for v1: the in-app "AI overview" panel** — the skill *trigger* into the CRM is in scope (goal 4); a rich summary surface in the dashboard is not.
- **Rationale**: the four founding goals are reliable record+transcribe, reliable Hebrew–English, backup, and meeting→skill→CRM automation. The spine covers 1–3 (modulo a live reliability proof); goal 4 (CRM auto-trigger) is entirely unbuilt, and the app isn't yet installable/onboardable by non-dev teammates (it runs from `make run-mac` only). P0 makes it real and trustworthy; P1 closes the value loop and adds the video the VC workflow wants; AI-overview is deferred to keep v1 lean.
- **Alternatives considered** (Yuval chose "pull video up"): installable+reliable → value loop → video → polish (recommended baseline); value loop before distribution; reliability-only first. Chose: keep P0 the adoption gate, but elevate video into P1.
- **Consequences**: video-in-P1 pulls its ripples forward — the Screen-Recording TCC grant (onboarding), a storage retention cap, and an ADR-010 consent revisit all move up. Updates `IMPLEMENTATION_PLAN.md`; **supersedes the prior Phase 3–6 *sequencing*** (not the technical designs in those ADRs). The original four founding goals + five live-flagged gaps (Drive folder UX, "unknown company" naming/edit, real installable app, Claude/CRM auto-trigger, video) are all mapped into P0–P2.

### 2026-06-15 — App shell is a hybrid: persistent Dock app + menu-bar tray (amends ADR-001/ADR-009)
- **Agent**: Claude Code (directive from Yuval)
- **Decision**: The app is BOTH a real Mac app (persistent **Dock icon**, appears in Cmd-Tab, opens the **dashboard** window) AND a menu-bar **"tray"** item for quick *Record-now* — not a pure menu-bar agent. Implementation: flip the app target from `LSUIElement=true` to `LSUIElement=false` (regular activation policy → Dock icon), keep the existing `MenuBarExtra`, set `applicationShouldTerminateAfterLastWindowClosed=false` so closing the dashboard keeps the recorder + menu-bar item alive in the background, make a Dock-click (re)open the dashboard (`applicationShouldHandleReopen`), and have launch-at-login come up **quietly** (background/menu-bar, no dashboard pop) so every login doesn't throw the window in the user's face.
- **Rationale**: Yuval wants it to feel like an actual installed app (Dock presence + dashboard) while keeping the one-click record affordance always at hand in the menu bar. A *persistent* Dock icon — not the appear-only-while-a-window-is-open dynamic-policy trick — is what "an actual mac app with icon" means here.
- **Alternatives considered**: keep `LSUIElement=true` + dynamically switch `NSApp.setActivationPolicy(.regular)` only while the dashboard is open (Dock icon appears/disappears) — rejected: Yuval wants a *persistent* Dock presence. Pure menu-bar agent (status quo) — rejected per this request.
- **Consequences**: amends ADR-001 ("menu-bar `LSUIElement`, no Dock icon") and ADR-009 (lifecycle). Must ensure closing the dashboard never quits the recorder, and that login-launch is unobtrusive. Lands in **P0** (app shell) — see `IMPLEMENTATION_PLAN.md` → "Road to a team-ready v1". Cheap (the dashboard window + "Open Dashboard"/fronting plumbing already exists from PR #7).

### 2026-06-15 — Packaging/distribution deferred to a final "Ship" phase (done last)
- **Agent**: Claude Code (decision by Yuval)
- **Decision**: Move the distribution work — Developer-ID signing, notarization, the drag-to-Applications `.dmg`, launch-at-login, and Sparkle — **out of early P0 to a final "Ship" phase done last** (after the rest of P0 + P1). The remaining pre-ship P0 work is the onboarding/TCC wizard + the reliability pass; then P1 (value loop + video); then Ship. Documented in `IMPLEMENTATION_PLAN.md` → "Road to a team-ready v1" (new **Ship** section) + the runbook `docs/distribution-setup.md`.
- **Rationale**: set up the signing/notarization pipeline **once**, when the app is feature-complete, instead of re-fighting it as the app changes. Yuval isn't ready to enrol in the Apple Developer Program yet (the gate — only an `Apple Development` cert exists, no Developer ID; the app can't use the App Store given its private TCC SPIs). Development + live testing continue fine on the current Apple Development signing — only *distribution to other Macs* needs Developer ID.
- **Consequences**: the team can't install the app until the Ship phase, so interim verification stays on Yuval's dev Mac. **Launch-at-login rides with Ship** (a login item should register the *installed* `/Applications` app, not an ephemeral DerivedData debug build). Supersedes the earlier ordering where distribution was P0 #2. The Apple Developer enrollment is the one external prerequisite; runbook in `docs/distribution-setup.md`.

### 2026-06-15 — P0 reliability + P1 call video + Drive folder picker (branch `feat/reliability-video-drive-picker`)
- **Agent**: Claude Code (Yuval: "everything" — build all three, then a manual test script)
- **Decision**: Built three slices on one branch (3 atomic commits), each `swift test` + `make build-mac` green; **live behaviours pending Yuval's verification** (see `docs/manual-tests-reliability-video-picker.md`).
  1. **Reliability — bundle the Silero VAD + surface pipeline failures.** The ~865 KB Silero VAD ships as an app **Resource** and `ModelManager.provisionFromBundleIfNeeded()` copies it into the Models cache on first launch (SHA-256-verified) — so the partial-silence fix always applies with no network. The 1.6 GB Hebrew model still downloads. Pipeline failures (previously invisible — a failed job writes no metadata.json, so it never reached the index) now surface: `MeetingStore.markFailed()` (v2 migration adds `pipelineError`) upserts a `status:"failed"` row from `job.json` facts; `JobBridge` records it on every terminal failure path and posts `.jobBridgeDidFinish` so the dashboard reloads live; `failed()` is its own bucket; the row shows a red "failed" chip + the error + a Reveal-pipeline.log action.
  2. **Call video (ADR-002 amendment, the 2026-06-14 decision).** New `ScreenCaptureKitRecorder` films the **detected call app's window only** (video-only HEVC, `capturesAudio=false` — audio stays on the Core Audio dual-track path); it degrades to audio-only when Screen Recording is denied or no window is found. The detector now exposes the **real bundle id** (`AudioProcessProbe.strippedBundleID` + `DetectionState.callAppBundleIDs`) for the SCK content filter. `CaptureSession.start/stop` are **async** now (the video writer is finalized on stop). `PlaybackRenderer` muxes window-video + level-balanced audio into one **`meeting.mp4`** (else `audio.m4a`); the dashboard plays it via `VideoPlayer`; `job.json`/`metadata.json` carry `video`. Video is **on by default for calls** (`CaptureSettings.recordCallVideo`); **A/V is aligned at t=0** (both captured within the same Start — good enough; precise-timestamp alignment is a later refinement if skew shows).
  3. **Retention (rides with video).** `DriveSync` uploads the muxed `meeting.mp4` (preferred over raw tracks) and, **on by default** (`CaptureSettings.pruneRawTracksAfterBackup`, user-disablable), prunes the local raw `mic/system/video` files **once the merged file is safely on Drive** — never without a merged file present. Video makes recordings GB-scale, so this is the storage cap for v1 (a global cache-size cap is a later refinement).
  4. **Drive folder picker = the real Google Drive web view (Google Picker API in a `WKWebView`).** Replaces "pick a Shared-Drive root + auto-create IN Meetings/". The user browses My Drive + Shared Drives and picks **any** folder; the pick is bridged JS→Swift; `DriveAuth.chooseFolder` resolves which Shared Drive the folder lives in via `DriveClient.fileInfo` (`files.get?fields=driveId`) so `DriveSync` scopes its calls, then persists the `DriveLocation`.
- **External prerequisite (Drive picker)**: the Picker needs a **browser API key** provisioned in GCP project **1062382667236** (enable the *Google Picker API* + create a Browser key) — set `GOOGLE_PICKER_API_KEY` or `DriveConfig.pickerAPIKeyDefault`. Not a data-access secret (the OAuth token still authorizes access); it only loads the Picker UI. Until set, the sheet shows setup steps instead of a blank page. The WKWebView loads under `pickerOrigin` (`https://localhost/`) — leave the key unrestricted or allow that referrer. (Mirrors the Calendar-API-enablement gotcha.)
- **Rationale**: keeps the moat intact (dual-track audio attribution unchanged — video is a *separate* SCK video-only stream, not the mila mono-mix path we rejected). Window-only capture keeps unrelated screen content out (ADR-010). The Picker is the user's explicit ask — a real Drive web view, not a hand-rolled tree browser; `files.get` for the driveId avoids needing the Shared-Drive id from the Picker JS.
- **Consequences**: video reverses the call path's "no Screen Recording TCC" posture (ADR-002/P3) — calls now need the **Screen Recording** grant (added to `Permissions` + Info.plist `NSScreenCaptureUsageDescription`); **revisit ADR-010 consent for default-on video (counsel)**. New Core: `ScreenCaptureKitRecorder`, `CaptureSettings`; new app file `DriveFolderPickerSheet`. Tests: **Core 76 (+19)**, pipeline 54, `make build-mac` green, app launches clean. **Amends ADR-002 (video track), ADR-006 (picker-chosen folder + retention), ADR-009 (Screen Recording onboarding)** — fold into those ADR files when convenient. ⏳ **PENDING LIVE VERIFICATION** on a real call (capture/diarization/video playback), a real Google sign-in (picker render), and the GCP key.
- **LIVE-VERIFIED 2026-06-16**: Drive folder picker (Google Picker web view) works on a real account; the recorded call produced a real 31 MB `meeting.mp4` (SCK capture + mux end-to-end). **Crash fixed**: opening a video meeting aborted the Swift runtime via SwiftUI `VideoPlayer` (`_AVKit_SwiftUI` `getSuperclassMetadata` fatalError) — replaced with a wrapped AppKit `AVPlayerView` (`fix(app)`), verified by a computer-driven click. The Google Picker browser key is provisioned but **deliberately kept out of git** (held in the working tree; not in PR #10). Merged path → **PR #10** (`https://github.com/YuvInv/IN-meetings/pull/10`). One gap surfaced: the recorded video's **A/V was out of sync** → see next entry.

### 2026-06-16 — A/V sync: unified ScreenCaptureKit capture for video calls (amends ADR-002, "Path A")
- **Agent**: Claude Code (Yuval chose Path A after a web-research review of how recorders sync A/V)
- **Problem**: the first video implementation captured mic (AVAudioEngine), system (Core Audio tap), and SCK video on **three independent clocks** and merged them at **t=0** in `PlaybackRenderer` — so a real call's `meeting.mp4` was **out of sync** (a fixed start-offset, since SCK video starts later than the audio, plus slow multi-clock drift). Confirmed against industry practice (OBS/Loom/Apple sample code): the cure is one capture clock + real presentation timestamps, never a t=0 merge.
- **Decision (Path A)**: for the **video-call path only**, capture everything through **one `SCStream`** — `.screen` (window video, HEVC) + `.audio` (system = "Them") + `.microphone` ("Me") — all on SCK's single clock (`captureMicrophone`, macOS 15+; app is 26). Each audio type writes to its **own** file (`mic.wav`/`system.wav`); writing both audio types to one sink is the documented `captureMicrophone` corruption pitfall. The **on-disk contract is unchanged** (`mic.wav` + `system.wav` + `video.mov` → muxed `meeting.mp4`), so the Python dual-track ASR/diarization + Drive upload are untouched — the **Me/Them moat is preserved**, now sourced from one synced stream. `PlaybackRenderer` muxes each audio track at its **real first-PTS offset** (not t=0). **In-person + call-without-video keep the existing path** (AVAudioEngine mic + Core Audio tap, no Screen Recording).
- **Amends ADR-002**: audio for a video call now comes from SCK, not the Core Audio tap. ADR-002 chose taps to avoid the Screen-Recording grant — moot here since video already requires it. The tap + AVAudioEngine path stays for the no-video cases **and as the fallback** if the unified SCK capture can't start (the call still records, audio-only). Old capture code kept intact = easy revert.
- **Alternatives considered**: (B) keep three captures but stamp each start against a common host clock + offset the mux — fixes start-skew but not multi-clock drift (rejected as a stopgap); a single mixed audio track written at capture (rejected — real-time mixing, and we want separate tracks for ASR anyway).
- **Consequences**: `ScreenCaptureKitRecorder` rewritten (video-only → unified); `CaptureSession` branches (unified vs audio path) + `Result` carries the A/V offsets; `PlaybackRenderer` is offset-aware. **77 Core tests** (+offset-mux test), `make build-mac` green (commit `71d2808`, on the same branch, **not yet pushed to PR #10**). ⏳ **PENDING LIVE TEST**: that SCK window-scoped `.audio` captures "Them" as cleanly as the tap, and that A/V is now in sync on a real Google Meet (+ both sides still transcribe). The `captureMicrophone` path has had bugs (separate writer files avoid the known one) — verify on a real call.
- **A/V LIVE-VERIFIED 2026-06-16**: the unified-SCK capture worked great on a real 22-min call. Two size fixes followed (`c41a5c0`): meeting.mp4 was 1.86 GB (3420×1970 @ 10.8 Mbps — 2×-retina + no bitrate cap) → cap **~720p / 2 Mbps HEVC / 15 fps / mono** + **passthrough-mux** (copy HEVC, no re-encode) + prune video.mov after mux → **~300 MB**. And **speaker naming** (`ddc449f`): manual one-tap assign (diarized Speaker N → an attendee or custom) persisted to transcript.json + Drive — full auto-naming needs voice-ID (a separate future project). All on **[PR #11](https://github.com/YuvInv/IN-meetings/pull/11)** (PR #10 merged).

### 2026-06-16 — Claude auto-trigger = headless `claude -p` + an app-bundled recipe (amends ADR-008; CRM on hold)
- **Agent**: Claude Code (architecture decisions by Yuval, design review this session)
- **Decision**: The "meeting → Claude skill" automation (goal #4) is built as a **headless `claude -p`** run the app spawns on pipeline-done, fed an **app-bundled recipe + house-style** as the system prompt — **not** a globally-installed skill, **not** the raw Anthropic API, **not** the Claude chat app. It writes `<meeting>/summary.md`, which the dashboard shows + Drive syncs. **Auto-on-finish** (only writes a file → no external action → no review gate). Spec: `docs/superpowers/specs/2026-06-16-saventa-summary-autotrigger.md`.
- **Why headless `claude -p`** (Yuval chose after a design review): the only surface that (a) the app can auto-trigger, (b) reuses the existing skill/house-style instructions, and (c) could later act via MCP. The **raw API** would force re-implementing the skill in app code (throws away the vc-context investment). The **Claude chat app / claude.ai** has no clean "auto-open a conversation with this transcript + skill" trigger — brittle UI automation. The summary living in the app + Drive beats a chat tab; a connector for the "chat with the summary" experience is a possible later complement.
- **Why bundle the recipe** (Yuval's call): a pure-instructions skill (no MCP/API) gains nothing from the install machinery and costs three risks — a per-machine install, plugin-overwrite (an update wipes edits), and version drift. The app **vendors** the recipe (the `saventa-summary` SKILL.md, local-folder mode) + the **non-CRM** vc-context (`critical-analysis`, `writing-style`, `investment-thesis`, `josh-preferences`, `anti-patterns`, `example-summary`, `style-analysis`) into its Resources and feeds them via `--append-system-prompt`. Only per-Mac dependency left: `claude` installed + logged in.
- **Sevanta/CRM posting is ON HOLD** — the original goal-4 endpoint (post the summary to Sevanta) is deferred; the summary lands in the app + Drive only. CRM would reintroduce a live config dependency (the Sevanta MCP + API key) that can't be bundled as text.
- **Also this session**: edited the installed `saventa-summary` plugin (the only on-disk copy, under `~/Library/Application Support/Claude/.../skills/saventa-summary/SKILL.md`) to accept a **local meeting folder** input + write `summary.md` — so it works on our context package, not just a Timeless URL. The bundled-recipe approach supersedes *depending* on that install, but the edited SKILL.md is the recipe to vendor.
- **Alternatives considered**: one-click-with-review trigger (kept for the future CRM step, unneeded for a file-only summary); the coordinated `--package` skill-install in `~/repos/claude-skills` (ADR-005 part 2 — superseded by bundling); meeting-type routing (skip internal/HR) — deferred (M1 summarizes all calls with an off-switch).
- **Consequences**: new Core `SummaryRunner` + a `summaryState` column (migration v3) + a dashboard "Summary" panel + a `summary.md` package file (synced via `DriveSync.textFileNames`). Amends ADR-008. **Not built yet** — design only. ⏳ **live-verify**: `claude -p` reliably runs the bundled recipe + writes `summary.md` headless under `--permission-mode acceptEdits`.

### 2026-06-16 — Roadmap: auto-stop → must-have; audio-file upload (must-have, not P0); user-defined post-meeting skills (future)
- **Agent**: Claude Code (requirements from Yuval)
- **Decision**: Three scope/priority changes to "Road to a team-ready v1":
  1. **Auto-stop when a meeting ends is now a v1 MUST-HAVE** (was a P2 "pulled-up" item). Promoted into **P1**. The mechanism stays the 2026-06-14 decision — a **debounced prompt, never a silent stop** (fires on the detector's armed→idle edge, rides out ~10–15 s of network blips; if ignored, keep recording — never silently drop a meeting). Still needs a short design pass first: **research how other recorders detect "meeting ended"** (call-app audio-process exit, call-window close, calendar end-time) before building.
  2. **Upload an audio file for transcription — new MUST-HAVE, explicitly NOT P0.** A user can feed an **existing audio file** (e.g. a meeting recorded on their phone) into the app → it runs the normal pipeline (ASR → post-correct → diarize → context package) → dashboard + Drive, with **no live capture**. Reuses everything downstream of capture. Design later (needs an import affordance + a synthetic `job.json` pointing at the imported file; profile = mic-only/single-track by default, since an imported file has no separate system track).
  3. **User-defined post-meeting "skills" — FUTURE, complex, NOT designed yet (explicitly deferred).** A Settings surface where each user describes to Claude what to do with a transcription — multiple summary types / workflows / schedules. **Generalizes** the saventa-summary auto-trigger (goal 4), which is the first, **hardcoded** instance. Recorded as a direction only; do **not** design or build it now.
- **Rationale**: (1) auto-stop is core to "don't lose and don't babysit a recording" reliability, so Yuval lifted it to must-have. (2) not every meeting is captured live (phone recordings, externally-recorded calls); file import makes the transcription + package + summary value reachable for those too — but it's a v1 *value* feature, not part of the P0 adoption gate. (3) the per-user-defined-skills idea is the natural evolution of the single bundled recipe, but it's a large design (prompt-management UI, multiple/scheduled workflows, safety), so capture the intent and ship the hardcoded saventa-summary first.
- **Consequences**: P1 gains auto-stop (must-have) + audio-file import (must-have); a new "Future / post-v1" bucket holds user-defined skills. The saventa-summary auto-trigger (building this session) is the concrete first step toward #3. No ADR rewrite yet — auto-stop will amend ADR-002 (stop logic) when designed; audio-import will touch the capture / `JobBridge` entry path; user-defined-skills will revisit ADR-008 when it's designed. `IMPLEMENTATION_PLAN.md` updated.

### 2026-06-22 — Onboarding / TCC permission wizard (implements the ADR-009 onboarding item)
- **Agent**: Claude Code
- **Decision**: Built the P0 first-run wizard (the adoption gate). A stepped Liquid Glass window walks **Microphone → System Audio Recording → Screen Recording → Google sign-in**, with plain-language explanations, live status pills, and graceful degradation. **Every step is skippable** (Continue always advances; granting is optional) so the user always reaches the dashboard. **Screen Recording is included in first-run with a restart at the end** (its grant only takes effect on relaunch — the Done step offers "Restart IN Meetings" so the first call already has video) — Yuval's call. The `claude`-CLI check is a non-blocking info row on Done, not a step. Architecture: pure Core **`OnboardingChecklist`** (`outstanding`/`isSetUp`, unit-tested) + **`Permissions.provokeSystemAudioPrompt()`** (a short-lived whole-system tap fires the System-Audio TCC prompt — no live call needed) + app **`OnboardingModel`** (@Observable, persists `onboarding.completed`) + **`OnboardingWindow`**/**`OnboardingStepView`**. First-run auto-opens the wizard (fronted) in `AppDelegate`; a menu **"Set up IN Meetings…"** re-runs it any time.
- **Rationale**: Without a guided first-run, non-dev teammates hit a wall of unexplained TCC prompts — the single biggest adoption blocker. Keeping the "what's missing / is it usable" logic in a pure Core type makes it testable and lets the same predicate drive both the recap and a future dashboard nudge. The four grants have very different mechanics (mic has a clean API; system-audio has none and must be provoked by a tap; screen-recording needs a relaunch; Google is OAuth), so the step chrome is uniform but each step's action differs.
- **Alternatives considered**: (a) Yuval chose **include Screen Recording + restart-at-end** over deferring it to first-video-record (rejected — the first call would be audio-only until a manual relaunch) and over a no-restart note (rejected — relies on the user restarting unprompted). (b) Per-browser Automation/Apple-Events grants + EventKit (ADR-009 lists them) are **out of scope** — we detect calls via Core Audio (not tab URLs) and calendar rides the Google OAuth, so neither is needed. (c) An inline Drive-folder picker in the Google step was descoped to a "pick it in Settings" note to keep onboarding lean.
- **Consequences**: Implements the ADR-009 onboarding-wizard item (minus the out-of-scope grants). Core tests **+6** (`OnboardingChecklistTests`); `make build-mac` green. Spec: `docs/superpowers/specs/2026-06-22-onboarding-tcc-wizard-design.md`; manual-test checklist: `docs/manual-tests-onboarding.md`.

### 2026-06-22 — Live-test finding: macOS 15/26 has no separate "System Audio Recording" grant (onboarding 4→3) + first-run fixes
- **Agent**: Claude Code (live-tested by Yuval, fresh user account + DMG install)
- **Finding (the load-bearing one)**: **On macOS 15/26, system-audio capture is authorized by the "Screen & System Audio Recording" grant** (the renamed Screen Recording) — there is **no standalone "System Audio Recording" category** in Privacy & Security (Yuval confirmed it's absent), and **empirically confirmed**: with only Screen Recording granted on a clean TCC state, the Core Audio process-tap "Them" track records non-silent audio. The old code/comments assumed the macOS-14.x model (a distinct system-audio grant with no read API); that's outdated for our team floor (macOS 26). This corrects the `2026-06-22` onboarding entry above (which had a separate System-Audio step + `provokeSystemAudioPrompt`).
- **Changes**: (1) **Dropped the System-Audio onboarding step (4 grants → 3)**; relabeled the screen step **"Screen & System Audio Recording"** ("records the call window + the other participants' audio"). Removed `OnboardingStep.systemAudio` + `Permissions.provokeSystemAudioPrompt()` (the create-a-tap "provoke" was unreliable AND unnecessary). (2) **Fixed a two-button trap**: each step now has a *single* prominent grant button; the footer is a quiet **"Skip for now"** until the grant is made, then a prominent **Continue** (previously two competing primaries let users skip the grant by accident). (3) **Settings deep-links + re-check on return** (`didBecomeActive`/step-change refresh) so a missed prompt is recoverable. (4) **Model visibility + management**: the ~1.5 GB Hebrew-model download now shows in the wizard (Welcome + Done) instead of silence, and Settings → Models gains the **path + size + Reveal in Finder + Delete / Re-download** (`ModelManager.fileURL`/`installedSizeBytes`/`delete()`/`redownload()`).
- **Consequences**: Onboarding is now Mic → Screen & System Audio → Google (+ Welcome/Done). Core tests **95** (removed the obsolete system-audio test); `make build-mac` green. Updates `Permissions.swift` doc (macOS-26 grant model). Still ⏳ a fully-clean-TCC live run to watch all three prompts fire + the relaunch. Branch `feat/onboarding-tcc-wizard` (PR #14).

### 2026-06-22 — Auto-stop on meeting end: debounced visible-countdown auto-stop (amends ADR-002; supersedes the 2026-06-14 keep-recording-if-ignored choice)
- **Agent**: Claude Code
- **Decision**: Built the P1 must-have auto-stop. On the detector's **`armed → idle`** edge while recording, a debounced **visible countdown** card ("Meeting ended — stopping in {n}s…") floats; if not cancelled it auto-stops + runs the normal pipeline. **Trigger = call-app audio-process exit** (the existing `CallDetector` Core Audio bidirectional-I/O signal) — the symmetric counterpart of the idle→armed start-prompt; no new detection mechanism (this answers the roadmap's "research meeting-end detection" ask). Architecture: a pure tick-driven Core state machine **`AutoStopArbiter`** (debounce → countdown → `.stopNow`, unit-tested) + a thin app-layer **`MeetingEndCoordinator`** (1 s timer, floating non-activating `NSPanel`) + a Liquid Glass **`MeetingEndOverlay`** card mirroring the start card. Defaults: **debounce 12 s, countdown 30 s** (injectable). `MeetingDetectionSettings.autoStopEnabled` (default **on**) + a menu-bar toggle next to the start-prompt toggle. **Never silent**: a stop only ever happens through the visible countdown; disabled → no auto-stop; card-can't-present → keep recording.
- **Rationale**: Reusing the proven start-side detection signal + the same coordinator/card pattern keeps the feature small, consistent, and risk-free on the detection side. Keeping the timing logic in a pure Core value type makes every debounce/countdown/cancel/re-arm path deterministically testable (the project bans `Date.now()` in tests), so only the visual + live flow need manual verification.
- **Alternatives considered**: (a) Yuval's chosen behavior **visible countdown → auto-stop** over the original 2026-06-14 "keep recording if ignored" (rejected: leaves hours of post-meeting silence) and over a VAD-gated "stop only on confirmed silence" (rejected for v1 — more logic, depends on the live silence check). (b) Folding both edges into the existing `MeetingPromptCoordinator` (rejected — one unit doing start-prompt + stop-countdown + two card types violates single-purpose). (c) Calendar-end-time / call-window-close triggers (deferred — the audio edge is sufficient and already proven).
- **Consequences**: **Supersedes** the 2026-06-14 auto-stop sub-decision (keep-recording-if-ignored) and **amends ADR-002** (stop logic gains the debounced visible-countdown auto-stop). "Keep recording" cancels the current countdown; a fresh `armed → idle` re-offers (Yuval). Core tests **90 → 100** (9 arbiter + 1 setting); `make build-mac` green. Spec: `docs/superpowers/specs/2026-06-22-auto-stop-design.md`; manual-test checklist: `docs/manual-tests-auto-stop.md`. **⏳ Live-verify on a real call** (the countdown + auto-stop + cancel paths) before merge.

### 2026-06-22 — Calendar-anchored audio import: single-track, assisted speaker labeling, provenance in job.json (not metadata)
- **Agent**: Claude Code (brainstormed + approved by Yuval)
- **Decision**: Built the "import an external recording + bind it to a calendar event" feature (reshapes the old P1 "upload an audio file" must-have). A right-side **day-agenda inspector** on the dashboard (`CalendarPanel` + `CalendarPanelModel`) lets the user page days, pick an event, and **upload a recording assigned to it**; the event's start/end/attendees/company enrich the package. Key decisions: (1) **Single mixed track → `profile:"inPerson"`** (the only pipeline profile that diarizes one track into N speakers), so an import's `metadata.meeting.type` is `"in_person"` even for a call. (2) **Assisted speaker labeling, not voice-ID**: diarization separates Speaker 1..N; the pinned event's attendees flow into `metadata.attendees` and the existing `MeetingDetailView` speaker chips offer them as one-tap labels — **no detail-view change**. Automatic identity→voice mapping (voice-ID) is explicitly out of scope. (3) **Provenance rides in `job.json`** (a `source:"imported"` key written by `ImportJob.make`, read back by `MeetingStore.indexPackage`) so the **frozen `metadata.json` schema (ADR-005) is untouched**; `calendarEventId` comes from the existing `metadata.meeting.calendar_event_id`. (4) The event is bound via a **`context.input.json` pinned to that one event** (`CalendarContext.writePinnedInput`) with the hint window = the event's own start/end, so the Python `match_event` overlap selects it — **the pipeline is reused unchanged**. (5) **Event-preferred with a no-event fallback** (footer "Upload without an event…"). (6) All logic lives in **`INMeetingsCore`** (view model, importer, audio normalizer, job builder — unit-tested); only the SwiftUI view + dashboard wiring are app-target.
- **Rationale**: Binding to a calendar event recovers the rich context a live capture gets for free, at a meeting-centric entry point, while honoring the no-bot/on-device constraints. Reusing `inPerson` + the pinned-context path means **zero pipeline/schema changes**. Keeping voice-ID out keeps v1 shippable (it's a separate hard project). Logic-in-Core keeps it testable without a UI/AVFoundation/Python harness.
- **Alternatives considered**: (a) a heuristic "me vs them" auto-label (rejected — an uploaded mixed file has no separate mic track, so "me" can't be known reliably); (b) adding a `source` field to `metadata.json` (rejected — would touch the frozen ADR-005 contract; job.json carries it instead); (c) overriding `metadata.type` to `"call"` for imported calls via a new pipeline field (rejected for v1 — single-track reality is honestly `inPerson`; the "Imported" badge carries provenance); (d) audio-first / always-prompt-for-event-after (rejected — calendar-first fits the common case).
- **Consequences**: New `MeetingRecord` columns `source` + `calendarEventId` (**migration v4**). Imports are processed `inPerson` → `type:"in_person"`; **auto-summary stays call-only** (imports use the existing manual Summarize). Imported **video** is audio-only (no player) in v1; re-assigning an import to a different event is out of scope. **Imports are blocked while a live pipeline job is in flight** — `JobBridge` is a shared singleton with a single status watcher, so a concurrent `enqueueImport` would orphan the running job's indexing; `RecordingStore.importRecording` guards on `jobBridge?.phase`. Core tests **121** (+13 across store/calendar/import/model), pipeline **56** (+2 import contract), `make build-mac` green. Spec: `docs/superpowers/specs/2026-06-22-calendar-upload-context-design.md`; plan: `docs/superpowers/plans/2026-06-22-calendar-upload-context.md`. **⏳ Live-verify**: pick an event → upload an `.m4a` → attendees appear as speaker labels; the no-event path; "✓ recorded" + open; a video container's audio extracts.
